<!-- This is a placeholder for the map-placeholder.jpg file -->
<!-- In a real implementation, this would be a proper JPG image -->
<!DOCTYPE html>
<html>
<head>
  <title>Map Placeholder</title>
  <style>
    body {
      font-family: 'SF Pro Display', -apple-system, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      background-color: #f5f5f7;
      color: #1d1d1f;
    }
    .container {
      text-align: center;
      max-width: 800px;
      padding: 20px;
    }
    .map-placeholder {
      width: 100%;
      height: 400px;
      background-color: #e0e0e0;
      border-radius: 12px;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0 auto 20px;
      position: relative;
      overflow: hidden;
    }
    .map-content {
      text-align: center;
      z-index: 2;
    }
    .map-icon {
      font-size: 60px;
      margin-bottom: 20px;
      color: #ff3b30;
    }
    .map-grid {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: linear-gradient(#cccccc 1px, transparent 1px), 
                        linear-gradient(90deg, #cccccc 1px, transparent 1px);
      background-size: 20px 20px;
      opacity: 0.5;
    }
    .map-pin {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 30px;
      height: 30px;
      background-color: #ff3b30;
      border-radius: 50% 50% 50% 0;
      transform: translate(-50%, -50%) rotate(-45deg);
      box-shadow: 0 0 10px rgba(0,0,0,0.2);
    }
    .map-pin:after {
      content: '';
      width: 14px;
      height: 14px;
      margin: 8px 0 0 8px;
      background-color: #ffffff;
      position: absolute;
      border-radius: 50%;
    }
    .map-circle {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 100px;
      height: 100px;
      border-radius: 50%;
      background-color: rgba(255, 59, 48, 0.2);
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0% {
        transform: translate(-50%, -50%) scale(0.8);
        opacity: 0.7;
      }
      70% {
        transform: translate(-50%, -50%) scale(1.2);
        opacity: 0;
      }
      100% {
        transform: translate(-50%, -50%) scale(0.8);
        opacity: 0;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="map-placeholder">
      <div class="map-grid"></div>
      <div class="map-circle"></div>
      <div class="map-pin"></div>
      <div class="map-content">
        <div class="map-icon">📍</div>
        <div style="font-weight: bold; font-size: 24px; color: #1d1d1f;">Format Disc</div>
        <div style="font-size: 16px; color: #1d1d1f;">Ilica 123, 10000 Zagreb</div>
      </div>
    </div>
    <h1>Map Placeholder</h1>
    <p>This is a placeholder for the map-placeholder.jpg file. In a real implementation, this would be an actual map showing the location of Format Disc.</p>
    <p>For the actual website, consider using Google Maps or another mapping service to provide an interactive map with directions to your location.</p>
  </div>
</body>
</html>