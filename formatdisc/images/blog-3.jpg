<!-- This is a placeholder for the blog-3.jpg file -->
<!-- In a real implementation, this would be a proper JPG image -->
<!DOCTYPE html>
<html>
<head>
  <title>Blog Image 3 Placeholder</title>
  <style>
    body {
      font-family: 'SF Pro Display', -apple-system, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      background-color: #f5f5f7;
      color: #1d1d1f;
    }
    .container {
      text-align: center;
      max-width: 600px;
      padding: 20px;
    }
    .image-placeholder {
      width: 100%;
      height: 300px;
      background: linear-gradient(135deg, #34c759, #007aff);
      border-radius: 12px;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0 auto 20px;
      color: white;
      font-weight: bold;
      font-size: 24px;
      position: relative;
      overflow: hidden;
    }
    .image-placeholder::before {
      content: "";
      position: absolute;
      width: 200%;
      height: 200%;
      background: rgba(255, 255, 255, 0.1);
      transform: rotate(45deg);
      animation: shine 3s infinite;
    }
    @keyframes shine {
      0% {
        transform: translateX(-100%) rotate(45deg);
      }
      100% {
        transform: translateX(100%) rotate(45deg);
      }
    }
    .blog-icon {
      font-size: 60px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="image-placeholder">
      <div>
        <div class="blog-icon">💾</div>
        <div>SSD vs. HDD</div>
        <div style="font-size: 16px; font-weight: normal; margin-top: 10px;">Usporedba tehnologija pohrane</div>
      </div>
    </div>
    <h1>Blog Image Placeholder</h1>
    <p>This is a placeholder for the blog-3.jpg file. In a real implementation, this would be a relevant image for the blog post comparing SSD and HDD storage technologies.</p>
    <p>For the actual website, use a high-quality image that visually represents both storage types or their comparison.</p>
  </div>
</body>
</html>